--- a/src/MappingFieldsHelper.php
+++ b/src/MappingFieldsHelper.php
@@ -261,7 +261,7 @@
    * @return array
    *   The list of mapping fields.
    */
-  public function getMappedFields(array $template = NULL) {
+  public function getMappedFields(?array $template = NULL) {
     $result = [];
     if (empty($template)) {
       return [];
@@ -300,7 +300,7 @@
    * @return array
    *   The list of mapping fields.
    */
-  public function getMappedFieldsFromContent(string $content = NULL, array $blocks = []) {
+  public function getMappedFieldsFromContent(?string $content = NULL, array $blocks = []) {
     $result = [];
     if (empty($content) && empty($blocks)) {
       return [];
--- a/gutenberg.module
+++ b/gutenberg.module
@@ -680,6 +680,13 @@
   $node_type_entity = $node->type[0]->entity;
   $text_fields = UtilsController::getEntityTextFields($node);
 
+  if (empty($text_fields)) {
+    \Drupal::logger('gutenberg')->warning('Gutenberg is enabled for %type, but no formatted long text field was found.', [
+      '%type' => $node_type,
+    ]);
+    return;
+  }
+
   // Mark form as Gutenberg enabled.
   $form['#gutenberg_enabled'] = TRUE;
   $form['#attributes']['data-gutenberg-enabled'] = TRUE;
@@ -1402,7 +1409,7 @@
  * @return bool
  *   Whether the entity is supported.
  */
-function _gutenberg_is_gutenberg_enabled(EntityInterface $entity = NULL) {
+function _gutenberg_is_gutenberg_enabled(?EntityInterface $entity = NULL) {
   if (!$entity) {
     return FALSE;
   }
